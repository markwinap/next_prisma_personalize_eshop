# CLI Commands
```sh
# install dependencies
npm install
# run
npm run dev
# migrate changes in db
npx prisma migrate
# generate prisma artifacts for DB client
npx prisma generate
# generate auth.js secret
npx auth secret --raw
```